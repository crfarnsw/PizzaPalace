@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

    <div class="row">
        <div class="col-md-12">
            <h1 class="text-center">Welcome to Pizza Palace</h1>
            <p class="text-center">
              <a class="btn btn-link" asp-page="Customer/Create">Sign up</a> or <a class="btn btn-link" asp-page="Customer/Login">Log In</a> to order a pizza.
            </p>
        </div>
    </div>
    <div class="row">
    </div>