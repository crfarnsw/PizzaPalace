@page
@model PizzaPalace.Pages.PizzaOrder.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PizzaOrder[0].OrderItem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PizzaOrder[0].Pizza)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PizzaOrder) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderItem.OrderItemId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pizza.PizzaId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PizzaOrderId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PizzaOrderId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PizzaOrderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
