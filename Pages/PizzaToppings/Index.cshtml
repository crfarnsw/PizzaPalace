@page
@model PizzaPalace.Pages.PizzaToppings.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PizzaToppings[0].PizzaOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PizzaToppings[0].Topping)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PizzaToppings) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PizzaOrder.PizzaOrderId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Topping.ToppingId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PizzaToppingId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PizzaToppingId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PizzaToppingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
