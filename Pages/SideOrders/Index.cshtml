@page
@model PizzaPalace.Pages.SideOrders.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SideOrder[0].OrderItem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SideOrder[0].Side)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SideOrder) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderItem.OrderItemId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Side.SideId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SideOrderId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SideOrderId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SideOrderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
